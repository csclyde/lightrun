package shader;

import h2d.filter.Filter;
import hxsl.Shader;

class LightGlow extends ScreenFilt{
        
    function fragment() {
        /*
        var x = input.uv.x - 0.5;
        var y = input.uv.y - 0.5;
        var dist = 1/(x*x+y*y);
        pixelColor = vec4(dist,dist,dist,1);
        */
    }
}

